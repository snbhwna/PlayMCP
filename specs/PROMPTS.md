# Prompts
@playwright Generate the best framework structure using Page Object Model, saving data if necessary in fixtures, save the credentials in a a playwright.env.json file

Optimize the tests and use BDD framework using cucumber

Create a readme.md file to explain how to run the tests, and the structure of the project.

Create a gitignore file to avoid committing the node_modules folder and the playwright.env.json file.

Create a CICD pipeline using Github Actions to run the tests.

#Later 


